/**
 * Shared fill for the hero bottom curve (`HeroSection` wave) and the top of the
 * category spotlight band so the transition reads as one surface, not white then grey.
 */
export const CATEGORY_PAGE_CURVE_SURFACE = "#f0f5f5";
